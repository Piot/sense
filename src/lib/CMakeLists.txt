# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(sense STATIC 
  sense_input_manager.c
  touches.c)

include(Tornado.cmake)
set_tornado(sense)

target_include_directories(sense PUBLIC ../include)


target_link_libraries(sense PUBLIC 
  basal)

